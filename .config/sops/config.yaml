# FILE: .config/sops/config.yaml
#
# DESCRIPTION:
# This file is used to configure the SOPS encryption tool. It specifies the
# AGE public key that should be used for encrypting files.
#
# Version: 2025-09-15 14:10:00 AEST
#
creation_rules:
    #
    # WHY: This section defines the encryption keys. You must add the PUBLIC age
    # key generated by `age-keygen` here. Any files encrypted with this configuration
    # will be decryptable by anyone holding the corresponding private key.
    #
  - key_groups:
      - age:
          - age1t0ge3nn3f09nwjfz2krsjmmyzauuv3294yr06hpak50jue7j2cfqwgjt8w
    # encrypted_regex: '^(data|stringData)$' # Optional. Encrypt only values under 'data' or 'stringData' keys (only applies to JSON or YAML files).
    # key_regex: '.*' # Optional. If omitted in YAML/JSON, defaults to encrypting all keys. Explicitly adding is clearer.
    # path_regex: \.env(\.sops)?\.yaml$ # Optional. Only encrypt files matching this regex (.env.yaml or .env.sops.yaml)